# Project Instructions

Read `PROJECT_CONTEXT.md` before making changes under `lang/go/mhibe` or discussing the M-HIBE experiments. It summarizes the relevant Codex session history, established protocol semantics, benchmark conventions, file families, known scaling limits, and unresolved work.

Treat `PROJECT_CONTEXT.md` as historical context, not as proof of the current implementation. Before changing code or reporting results, verify the relevant source file and current command output. Preserve existing benchmark variants unless the user explicitly asks to modify them; previous work intentionally created new variants instead of overwriting baselines.

